{% extends "base.html.twig" %}

{% block body %}

    <h3>Signature des méthodes SOAP disponibles</h3>
    {% if fonctionsDisponibles is defined %}
        <ul class="list-group">
            {% for fonctionDisponible in fonctionsDisponibles %}
                <li class="list-group-item">{{ fonctionDisponible }}</li>
            {% endfor %}
        </ul>

    {% endif %}

    {% if categorie is defined %}
        <p>id :{{ categorie.id }}</p>
        <p>libelle: {{ categorie.libelle }}</p>
    {% endif %}

{% endblock %}